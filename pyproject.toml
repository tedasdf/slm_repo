[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "slm"
version = "0.1.0"
description = "SLM training pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]